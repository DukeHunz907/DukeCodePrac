// write a program the recives double dumbers from the user and add them to a 
//vector until 0 is entered by the user. the print out all the numbers

#include <iostream>
#include <vector>
using namespace std;
int main(){
    vector<double> numbers;
    double num;
    cin >> num;
    while(true) {
        for(int i = 0; i )
    }

}