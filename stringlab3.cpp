//Longest Word: Write a program that receive a sentence and prints out length of the longest word in the string.
//The text has no punctuation and only words each separated by one space.
//Can you change the program to print out the longest word itself too? 
#include <iostream>
using namespace std;
int main () {
    string s, w;
    int maxLen =0;
    int i = 0;
    getline(cin, s);
    while (i < s.length()) {

    }
}