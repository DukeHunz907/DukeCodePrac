
#include <iostream>
using namespace std;
int main () {
    //cout << (int) 'a' << " " << (int) 'A' << " " << (int) 'z' << " " << (int) 'Z' << endl;

    string inputStr;
    getline(cin, inputStr);
    for (int i = 0; i < input.length(); i++) {
        if (inputStr[i] >= 97 and inputStr[i] <= 122 and (i == 0 or inputStr[i-1] == ''))

    }
}