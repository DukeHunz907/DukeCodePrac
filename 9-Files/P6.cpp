//File Character Count Write a C++ program that reads a text file named "characters.txt" and 
//counts the total number of characters (including spaces and special characters) in the file.

#include <iostream>
#include <fstream>
using namespace std;

int main() {
    ofstream("characters.txt");
    string s;
    getline(cin, s);
    
    
}